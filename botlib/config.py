#config.py stores items like variables,settings,etc to be used across the project, such as cmdList

#an list of active commands, their usage, syntax, and number of arguments that they require
cmdList = {
    #name, usage, syntax, requiredArgs
    '0BADGUY': ['Command intentionally missing logic for debug/test purposes.', '${{badguy}}', 0],
}
